// src/components/UserSubscription.tsx
import { Typography } from '@mui/material';
import React from 'react';

const UserSubscription: React.FC = () => {
  return <Typography>Trạng thái tài khoản của bạn</Typography>;
};

export default UserSubscription; // Thêm export để biến file thành module